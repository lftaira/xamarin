<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="XF.ControlesBasicos.ConfigPage">
    <ContentPage.Content>
        <StackLayout >
            <TableView>
                <TableView.Root>
                    <TableSection Title="Privacidade">
                        <SwitchCell Text="Aceita Receber Email?"  On="{Binding ReceberEmail}" />
                        <EntryCell Placeholder="Informe seu e-mail" Text="{Binding Email}" />
                    </TableSection>
                </TableView.Root>
            </TableView>
            <!--<Entry Placeholder="Informe seu e-mail" x:Name="txtEmail" On="{Binding Email}"/>-->
        </StackLayout>
    </ContentPage.Content>
</ContentPage>